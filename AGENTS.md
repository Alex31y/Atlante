# Repository Writing Rules

- Do not use em dashes (`—`) or en dashes (`–`) in repository prose, README copy, docs, release notes, comments, or UI text.
- Use plain ASCII punctuation instead: commas, periods, colons, parentheses, or ` - ` when a dash is genuinely needed.
